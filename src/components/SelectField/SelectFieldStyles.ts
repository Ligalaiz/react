import { css } from '@emotion/react';
import { color } from '@src/styles';

export const experianceSelect = css`
  width: 150px;

  color: var(--primary-color);

  outline: none;
  background-color: transparent;
  cursor: pointer;
`;
export const experianceItem = css`
  color: ${color['black-color']};
`;
